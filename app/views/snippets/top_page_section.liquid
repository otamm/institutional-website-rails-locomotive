---
title: Top page section

# position among sibling pages
# position: 1

# sets a redirection to the given url (301)
# redirect_url: "<url to a page or to a remote url>"

# content type that this page is templatizing
# content_type: "<slug of one of the content types>"

editable-elements:
#   'some_block/some_slug': "<text>"
#   'some_block/some_slug': "<relative path to the file under the public/samples folder>"
    'main-link': Contact
    'main-link-url': www.youtube.com
---
{% block 'featured_photo' %}
  {% capture caption %}
    {% editable_text "caption", format: "raw", rows: 3, hint: "Text displayed below the photo and in the ALT attribute." %}
      Gyeongbokgung, Seoul
    {% endeditable_text %}
  {% endcapture %}
  {% capture photo %}
  	{% editable_file "photo" %}
  		/samples/pages/gyeongbokgung-full.jpg
  	{% endeditable_file %}
  {% endcapture %}
  <div class="top-page-image-bg">
    <a href="{{ photo | resize: '800x' }}">
      <img src="{{ photo | resize: '400x' }}" alt="{{ caption }}" />
    </a>
    <ul id="header-links">
      {% for link in contents.headerlinks %}
        <li><a href="{{ link.redirect }}">{{ link.title }}</a></li>
      {% endfor %}
        <li><a href="{{ editable_text "main-link-url", format: "raw", hint: "The url for the main top link" }}www.youtube.com{{endeditable_text}}">{{ editable_text "main-link", format: "raw", hint: "The label for the main top link" }}CONTACT{{endeditable_text}}</a>
    </ul>
    <p>{{ caption }}</p>
  </div>
{% endblock %}